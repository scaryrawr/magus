# dependencies (installed via pnpm/Bun/npm)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage/
*.lcov

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
report.*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# Next.js
# Build output and caches
.next
.turbo
# Vercel project settings directory
.vercel
# Ensure Next.js type shim stays tracked
!**/next-env.d.ts

# Yarn Berry (v2+)
# Ignore everything in .yarn except the releaseable metadata
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
# If not using Zero-Installs, keep cache and install state out of git
.yarn/cache
.yarn/install-state.gz
.pnp.*
.pnp.loader.mjs
# pnpm specific
.pnpm-debug.log*
.pnpm

# Lage / Backfill caches
.backfill/
node_modules/.cache/lage/
node_modules/.cache/backfill/

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# OS / misc
Thumbs.db
bun.lockb

# Magus local data
.magus/
